<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <ImageView
        android:layout_width="180px"
        android:layout_height="180px"
        android:id="@+id/ivMap"
        android:background="@drawable/map_marker"
        android:onClick="onClickListener"
        android:layout_above="@+id/tvService"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/ParkingMap"
        android:id="@+id/tvMap"
        android:layout_below="@+id/ivMap"
        android:layout_alignRight="@+id/ivMap"
        android:layout_alignEnd="@+id/ivMap" />

    <ImageView
        android:layout_width="180px"
        android:layout_height="180px"
        android:id="@+id/ivService"
        android:src="@drawable/spanner_logo"
        android:layout_marginTop="44dp"
        android:onClick="onClickListener"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Veloservisi"
        android:id="@+id/tvService"
        android:onClick="onClickListener"
        android:layout_below="@+id/ivService"
        android:layout_alignLeft="@+id/ivService"
        android:layout_alignStart="@+id/ivService" />
</RelativeLayout>
